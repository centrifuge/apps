import * as React from 'react'
import { PageWithSideBar } from '../components/shared/PageWithSideBar'

export const PoolsPage: React.FC = () => {
  return <PageWithSideBar>Pools page (to implement)</PageWithSideBar>
}
